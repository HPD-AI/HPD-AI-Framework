# HPD-RAG.VectorStores.CosmosMongo

Azure Cosmos DB for MongoDB vector store provider for HPD-RAG.

## Install

```bash
dotnet add package HPD-RAG.VectorStores.CosmosMongo
```

## Use When

Use this package when you need the CosmosMongo vector store integration for HPD RAG.
