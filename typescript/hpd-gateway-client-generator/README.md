# @hpd/gateway-client-generator

Deterministic, fail-closed build-time generator for the fixed HPD.Gateway Admin
contract. It consumes a correlated `hpd-gateway-v1` generation snapshot and
emits dependency-free TypeScript contract artifacts. It does not perform HTTP
requests and is not the Gateway runtime client.

This package is under active Slice 3 implementation.
