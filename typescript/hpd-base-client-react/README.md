# @hpd/base-client-react

React bindings over the framework-neutral BASE query cache. Hooks accept the query operation itself;
transport, reconnect, identity partitioning, and optimistic reconciliation stay in `@hpd/base-client`.
