import {describe,expect,it} from 'vitest';
import {createGatewayDeclarationController} from '../src/declaration-state.ts';
import {everyEditableFieldHasRenderer,projectGatewayVisualFields,visualFieldNode} from '../src/visual-fields.ts';
describe('generated visual field projection',()=>{it('has a real renderer for every released editable behavior leaf',()=>expect(everyEditableFieldHasRenderer()).toBe(true));it('resolves only the active union branch and changes the one authored graph',()=>{const controller=createGatewayDeclarationController();controller.replaceRaw('{"schemaVersion":{"major":1,"minor":0},"canonicalizationVersion":1,"upstreams":[{"id":{"value":"up"},"endpoints":{"kind":"static","destinations":[{"id":{"value":"d"},"address":"https://before.test","hostOverride":"backend.test"}]}}]}');const fields=projectGatewayVisualFields(controller.snapshot().document.compatibleGraph!,null);expect(fields.some(value=>value.pointer.endsWith('/endpoints/destinations/0/address'))).toBe(true);expect(fields.some(value=>value.family==='discovery')).toBe(false);const address=fields.find(value=>value.pointer.endsWith('/address'))!;expect(controller.setAtPointer(address.pointer,visualFieldNode(address,'https://after.test')!)).toBe(true);expect(controller.snapshot().document.utf8Text).toContain('https://after.test');const host=projectGatewayVisualFields(controller.snapshot().document.compatibleGraph!,null).find(value=>value.pointer.endsWith('/hostOverride'))!;expect(controller.removeAtPointer(host.pointer)).toBe(true);expect(controller.snapshot().document.utf8Text).not.toContain('hostOverride');});});
