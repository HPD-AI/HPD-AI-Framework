# HPD Studio Core

The first supported, product-neutral HPD Studio module runtime. It owns closed
module composition, exact routing, optional quarantine, required failure,
owner-scoped contexts, tracked lifecycle cleanup, navigation fallback, and the
bounded authentication/principal seam.

The supported module set is frozen at startup. There is no dynamic plugin
loader, product selection, authorization cache, credential store, or legacy
Studio compatibility surface.
