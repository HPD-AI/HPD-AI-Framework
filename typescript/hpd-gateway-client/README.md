# @hpd/gateway-client

Generated, bounded standard-Fetch client for the HPD Gateway Admin API.

The package is preview software and follows Decision 0014. It exposes one
typed method per generated Admin operation and returns closed result unions;
HTTP, protocol, transport, and cancellation outcomes are not thrown.
