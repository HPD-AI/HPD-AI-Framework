# Third-party notices

The packaged `liblivekit_ffi.dylib` is built from LiveKit's
`livekit-ffi/v0.12.60` source at commit
`a4f41cdcae5214986ab4ac5a8cb8507e5cc7ee6e` and is licensed under
Apache License 2.0.

Upstream project: <https://github.com/livekit/rust-sdks>

The artifact was obtained from the signed `Livekit.Rtc.Dotnet` `0.1.3`
package solely as a pinned packaging input. That managed wrapper assembly and
its APIs are not dependencies of the resulting HPD runtime package.
