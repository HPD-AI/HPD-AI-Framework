<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        Hardened Runtime exceptions needed by .NET AOT self-contained binaries.

        disable-library-validation: allows the runtime to load its own bundled
        dylibs (e.g. libcoreclr.dylib) without them needing a matching team ID.

        allow-jit: some .NET GC / P/Invoke paths require executable writable
        memory even in AOT builds.

        No explicit TCC entries (screen-recording, microphone, camera) are
        needed here. macOS will prompt the user automatically the first time
        hpdos requests those resources, as long as the binary is code-signed
        with a valid Developer ID and a bundle identifier (com.hpd.hpdos).
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
</dict>
</plist>
